<div id="photos">
  <% @artists.each do |artist| %>
    <div class="artist">
      <% artist.photos.each do |photo| %>
        <%= link_to image_tag(photo.image), artist_path(artist.id) %>
      <% end %>
    </div>
  <% end %>
</div>
