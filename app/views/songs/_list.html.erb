<h3>Songs:</h3>
<ul id="songs-list">
  <% if @songs.present? %>
    <% @songs.each do |song| %>
      <li id="song-<%= song.id %>"><%= song.name %><button class="delete-button">x</button></li>
    <% end %>
  <% end %>
</ul>

<script>
  var artistId = <%= @artist.id %>;
</script>
